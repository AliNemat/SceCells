#include <vector>
#include <iostream>
#include "GeoVector.h"
using namespace std ; 

vector<double> updateSignal(const vector<CVector> & cellCentersHost, int cellMax, double MinX, double MaxX, double MinY, double MaxY, double dt, double InitTimeStage, double curTime) ; 
